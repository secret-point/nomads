import { StyleSheet } from "react-native";

export default StyleSheet.create({
  decimal: {
    fontSize: 32,
  },
  fraction: {
    fontSize: 16,
  },
  priceItemWrapper: {
    display: "flex",
    flexDirection: "row",
  },
  symbol: {
    fontSize: 32,
  },
});
